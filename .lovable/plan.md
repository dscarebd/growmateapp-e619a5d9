

## Plan: Rename "Boostly" to "Grow Mate"

Two files need updating:

1. **`src/pages/Splash.tsx`** (line 29): Change `"Boostly"` → `"Grow Mate"`
2. **`src/pages/Auth.tsx`** (line 63): Change `"Join Boostly today"` → `"Join Grow Mate today"`
3. **`index.html`**: Update `<title>` and Open Graph meta tags from "Lovable App" to "Grow Mate"

